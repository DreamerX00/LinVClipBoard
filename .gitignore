# Rust
/target/
**/*.rs.bk

# Tauri
/crates/linvclip-ui/node_modules/
/crates/linvclip-ui/dist/
/crates/linvclip-ui/src-tauri/target/
# Editor JSON schemas regenerated by tauri-build on every compile
/crates/linvclip-ui/src-tauri/gen/schemas/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# LinVClipBoard data (dev)
*.db
*.db-journal
blobs/

# Embedded API keys (never commit)
klipy.key

# Upgrade plan (internal planning docs)
upgrade-plan/

# Multi-platform porting plan (internal)
multiPlatformPlan.md

# Windows
*.exe
*.msi
!resources/.gitkeep
*.dll
*.pdb
target/windows/
*.nsi
installer-logs/
!windows/hooks/*.nsh

# Release artifacts (published by the CI release job, not stored in git)
/dist/
crates/linvclip-ui/src-tauri/binaries/

# Local release-notes working file (release.sh manages notes-$VERSION.md itself)
Release_description.md

# Deb packaging artifacts (standalone scripts, not used by build-deb.sh)
/install/deb-desktop.desktop
/install/deb-postinst.sh
